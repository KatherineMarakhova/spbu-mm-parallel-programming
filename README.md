# TASK 5. P2P-Chat via Sockets

Use multiple program windows to chat with users. 

Simple commands for the interface:

```
Press 'C' for connecting to the chat
Press 'M' for writing message
Press 'Q' to exit the chat
```
